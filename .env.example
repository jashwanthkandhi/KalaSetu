# Android configuration uses -PBACKEND_URL, not environment credentials.
# Configure server providers in backend/.env (see backend/.env.example).
# Never put provider keys in Android BuildConfig or resources.
